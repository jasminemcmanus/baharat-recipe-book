/* type effect */

/*
let speed = 90;
let headerText = document.getElementById('type-effect');

function typeEffect(element, speed) {
	let text = element.innerHTML;
	element.innerHTML = "";
	
	let i = 0;
	let timer = setInterval(function() {
    if (i < text.length) {
      element.append(text.charAt(i));
      i++;
    } else {
      clearInterval(timer);
    }
  }, speed);
}

typeEffect(headerText, speed) */